/**
 * Sermone Script 
 * 
 * @since 1.0.0
 */

import './scss/main.scss' 
import './nav-tab'

;( ( w, $ ) => {
  'use strict';

  
  const MediaTabFunc = () => {
    
  }

  /**
   * Ready 
   */
  const Ready = () => {

  }

  /**
   * DOM Ready
   */
  $( Ready )

  /**
   * Browser load completed
   */
  $( w ).on( 'load', () => {} )

} )( window, jQuery )