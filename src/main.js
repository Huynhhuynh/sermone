/**
 * Sermone Script 
 * 
 * @since 1.0.0
 */

import './scss/main.scss' 

/**
 * Single
 */
import './nav-tab'
import './share'

;( ( w, $ ) => {
  'use strict';

  /**
   * Ready 
   */
  const Ready = () => {

  }

  /**
   * DOM Ready
   */
  $( Ready )

  /**
   * Browser load completed
   */
  $( w ).on( 'load', () => {} )

} )( window, jQuery )